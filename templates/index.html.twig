{% extends 'base.html.twig' %}

{% block title %}Eve Toolkit Home{% endblock %}

{% block body %}

    {% for id, char in eve_api.getCharacters() %}
        <li><a href="{{ path('character.activate', { 'id': id }) }}">{{ char.name }}</a></li>
    {% endfor %}

{% endblock %}
